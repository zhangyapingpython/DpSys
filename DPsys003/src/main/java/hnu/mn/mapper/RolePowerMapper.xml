<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hnu.mn.mapper.RolePowerMapper">
	
	<!--根据角色ID查找权限  -->
	<select id="selByRoleId" resultType="hnu.mn.pojo.RolePower">
		select * from t_role_power where roleId=#{roleId};
	</select>
	
</mapper>